class Car {
  public id: number;

  public name: string;

  public price: number;

  constructor(_id: number, _name: string, _price: number) {
    this.id = _id;
    this.name = _name;
    this.price = _price;
  }
}

export default Car;
